name: deinSync
authors: [leNic]
version: ${project.version}
main: bz.dcr.deinsync.DeinSyncPlugin
depend: [dcCore, ProtocolLib]
commands:
  deinsync:
    aliases: [ds, sync]
    usage: /<command> update
    description: deinSync Command